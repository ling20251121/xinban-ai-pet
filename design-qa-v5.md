# v5.0.0 设计验收记录

**Final result: blocked**

日期：2026-08-12

## 已完成的静态与构建检查

- 登录页：学校用户名/密码、显示与隐藏密码、学生/教师入口、无手机号字段。
- 教师登录标签含默认收起的首次部署初始化入口；部署口令以密码框输入且不保存，仅可创建首个教师账号，成功后直接进入工作台。
- 学生首次进入：强制初始密码修改；显示监护人核验状态；未核验时禁止进入；学生本人明确同意后进入。
- 学生页脚提供“撤回同意并退出”：二次勾选确认后停止录音、转写与朗读，撤回同意并退出；明确不会自动删除已有内容，之后仍可登录导出或删除。
- 学生主流程：轻量心情签到 → 保存 → 15 分钟或 12 轮持续 AI 对话；聊天气泡、AI 身份、底部大输入框、300 字计数、语音转写、Qwen/设备主动朗读、结束/复制/删除/真人求助均已呈现。
- 教师工作台：班级创建、学生账号创建、年龄段、监护人核验、学生同意、启停状态、班级真实汇总、最少必要支持与安全事件；无默认示例数据，空数据时显示真实空状态。
- 教师账号页面只创建、展示匿名学校用户名，不输入或展示姓名/可识别显示称呼。
- 安全边界：普通聊天原文不进入教师页面；安全事件仅展示账号、班级、来源、事件码、时间及人工核对状态；“待教师查看”未写成“已通知”。
- 响应式静态核对：学生签到/对话和教师工作台分别覆盖 1050/720/460px 断点；关键触控目标为 44px 或以上；提供键盘焦点、跳转链接、ARIA live、reduced-motion。
- 已消除 v1-v4 旧样式与 v5 同名选择器的层叠覆盖风险，v5 规则在文件末尾再次限定到 `.student-app` / `.teacher-shell`。
- 定向 ESLint：通过。
- `pnpm run build`：通过；登录、学生、教师与全部 v5 API 路由均被构建识别。

## 浏览器视觉 QA 阻塞

本地生产预览已恢复，并用应用内浏览器实际打开登录页。DOM 验收确认学生/教师入口、学校用户名/密码、密码显示、无手机号字段以及 AI/真人安全边界正常呈现。在 360×800 视口下，实测 `scrollWidth=345`，未发现横向溢出。

但浏览器在执行 `Page.captureScreenshot` 时持续超时，而学生签到/持续对话和教师真实空状态又需要完成本地 D1 账号初始化。因此仍无法产生同视口截图与参考图做像素级并排比较。按视觉验收规则，最终结果保持 `blocked`，不把 DOM、构建或响应式数值冒充为截图 QA 通过。

## 后续浏览器验收清单

浏览器连接恢复后，应按以下顺序复验：

1. 1440×900 登录页：左右分栏、dog.svg 位置、输入框和密码显示按钮。
2. 1440×1000 学生签到页：柔紫双栏、大留白、心情选择、右上语音按钮、大文本框和隐私说明。
3. 1440×1000 持续对话页：左侧宠物、右侧气泡、时间/轮次、工具栏、底部吸附 composer。
4. 360×800 持续对话页：无横向溢出、44px 控件、输入框与真人帮助入口不互相遮挡。
5. 1440×1000 教师无数据状态：侧栏、空统计、创建班级入口，确认没有虚构数据。
6. 键盘遍历、200% 缩放、`prefers-reduced-motion`、ARIA live 与紧急区域聚焦。
